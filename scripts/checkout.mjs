// dev tools - application -- local og session storage
// data is stored as key/value pairs and persists when closing the browser


// send data to local storage:
// localStorage.setItem("key", JSON.stringify(object, api or whatever));
// get data back:
// const getInfo = localStorage.getItem("key");
// convert to js:
// const backToJS = JSON.parse(getInfo);

// you can store api data in local storage. You can also store when the data was added.
// a bit too advanced for now.. :P
// get chatgpt to Single Responsibility Principle your code